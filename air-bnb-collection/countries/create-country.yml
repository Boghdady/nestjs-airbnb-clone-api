info:
  name: create-country
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/countries
  body:
    type: json
    data: |-
      {
        "name": "Jordan",
        "countryCode": "HRD"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
