info:
  name: create-country
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/countries
  body:
    type: json
    data: |-
      {
        "name": "Iraq",
        "countryCode": "IRQ"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
