info:
  name: create-city
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/cities
  body:
    type: json
    data: |-
      {
        "name": "Ismalia",
        "country": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
