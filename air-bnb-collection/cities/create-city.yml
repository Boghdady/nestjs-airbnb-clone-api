info:
  name: create-city
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/cities
  headers:
    - name: Authorization
      value: Bearer {{jwt}}
  body:
    type: json
    data: |-
      {
        "name": "اربيل",
        "country": "69d3a90a85613dfa2ba198f8"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
