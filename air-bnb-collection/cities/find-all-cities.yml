info:
  name: find-all-cities
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/cities
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
