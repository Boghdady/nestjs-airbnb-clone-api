info:
  name: login-as-admin
  type: http
  seq: 4

http:
  method: POST
  url: "{{URL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "admin@airbnb.com",
        "password": "pass123",
        "role": "admin"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("jwt", res.body.accessToken);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
