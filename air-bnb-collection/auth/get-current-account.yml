info:
  name: get-current-account
  type: http
  seq: 5

http:
  method: GET
  url: "{{URL}}/auth/me"
  headers:
    - name: Authorization
      value: Bearer {{jwt}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
