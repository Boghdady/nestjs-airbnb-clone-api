info:
  name: login-as-user
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "khalid@gmail.com",
        "password": "pass123",
        "role": "user"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("jwt", res.body.accessToken);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
