info:
  name: create-unit-category
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/unit-categories
  body:
    type: json
    data: |-
      {
        "name": "Villa",
        "icon": "url"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
